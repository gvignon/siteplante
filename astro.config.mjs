// @ts-check
import { defineConfig } from 'astro/config';

// Configuration Astro.
// Quand tu déploieras, remplace "site" par l'URL finale du site.
export default defineConfig({
  site: 'https://exemple.netlify.app',
});
